import random

number = random.randint(1,6)
nimekiri = [1,2,3]
sõna = "Tere"

näide = {
    "esimene" : number,
    "teine" :nimekiri[1],
    "kolmas" : sõna,
    "neljas" : random.randint(1,10)
    }